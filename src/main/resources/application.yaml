server:
  port: 8080
  address: 127.0.0.1

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ke-toan
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true


